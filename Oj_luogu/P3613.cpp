#include<bits/stdc++.h>
using namespace std;

// Use a list to record the data but to save the place
// just omit the empty node and use a element to record the 
// num of the each node and order them when insert them

int main(){
	int n, m;
	scanf("%d %d", &n, &m);
	int i = 0;
	while(i++ < m){
		int flag; 
		scanf("%d", &flag);
		if(flag == 1){
			int i, j;
			long long k;
			scanf("%d %d %ll", &i, &j, &k);

		}
		else if(flag == 2){

		}
		else exit(-1);
	}
	return 0;
}
