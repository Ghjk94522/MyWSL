#include<bits/stdc++.h>
using namespace std;

// Don't use the cout when you are not sure the type of the variable you want to print

int main(){
    char a; cin >> a;
    int t = 'A' - 'a';
    a += t;
    cout << a;
    return 0;
}